apiVersion: rabbitmq.com/v1beta1
kind: RabbitmqCluster
metadata:
  name: rabbitmq
spec:
  replicas: 3
  persistence:
    storageClassName: ""
    storage: 8Gi

  override:
    statefulSet:
      spec:
        template:
          spec:
            securityContext:
              fsGroup: 999
